A pulsing placeholder for the session-pending state in the header and login card.

```jsx
<Skeleton style={{ height: 32, width: 96, borderRadius: "var(--radius-lg)" }} />
```

Buttery only uses skeletons for auth/session resolution — data lists render empty-state copy instead.
