The form label. In practice you use `FieldLabel` (which wraps this) rather than `Label` directly.

```jsx
<Label htmlFor="household-name">Household name</Label>
```

Labels are sentence case, no colon, and describe the value ("Handle to invite", "Expires (days)").
