A 1px ink rule for separating sections inside a page or card.

```jsx
<Separator style={{ margin: "2.5rem 0" }} />
<Separator orientation="vertical" />
```

Used between the recipe header and the ingredients/instructions grid, and between an invite form and the invite list. Never used as a decorative divider — gingham bands do that job.
