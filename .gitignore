node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.mcp.json
.pnpm-store

# @atproto/lex generated TypeScript — regenerated from lexicons/ via `pnpm lex:build`.
# The source of truth (lexicons/*.json + lexicons.json manifest) IS committed.
/src/lexicons
